export interface LinearMaskSettings {
  angle: number;
  stop: number;
}

export interface RadialMaskSettings {
  rx: number;
  ry: number;
  posX: number;
  posY: number;
  innerStop: number;
  outerStop: number;
}
